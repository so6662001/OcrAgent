# OCR智能识别系统 — 架构设计文档

## 1. 项目概述

### 1.1 业务背景

企业在日常运营中需要处理大量纸质/电子单据（进货单、进项发票、询价清单、供应商价格表等），目前依赖人工录入，效率低且易出错。本系统旨在通过OCR技术实现单据自动识别与数据提取，大幅提升业务效率。

### 1.2 核心需求

| 需求项 | 描述 |
|--------|------|
| 万能识别 | 支持PDF、图片（机打/手写），处理模糊、扭曲等异常场景 |
| 可配置对象 | 单据类型及其字段在系统中可自定义配置 |
| 供应商适配 | 不同供应商的模板和字段名称差异需自动映射 |
| 智能分流 | 识别率100%自动入库，低于100%人工审核 |
| 批量处理 | 支持批量/单个文件识别 |
| 多端支持 | PC端 + H5移动端 |
| 高并发 | 支持1万并发 |

---

## 2. 系统架构

### 2.1 整体架构图（C4 — System Context）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          用户端 (PC / H5)                               │
│                    Vue3 + Element Plus / Vant                           │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │ HTTPS / WebSocket
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        API网关层 (Spring Cloud Gateway)                 │
│                   限流 · 鉴权 · 路由 · 负载均衡                          │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  文件服务         │ │  OCR识别服务      │ │  业务管理服务     │
│  (File Service)  │ │  (OCR Service)   │ │  (Biz Service)   │
│                  │ │                  │ │                  │
│ · 文件上传/存储   │ │ · 图像预处理      │ │ · 对象/字段配置   │
│ · 格式转换       │ │ · OCR引擎调度     │ │ · 供应商管理      │
│ · PDF解析        │ │ · 结果结构化      │ │ · 识别任务管理    │
│ · 批量管理       │ │ · 置信度评估      │ │ · 人工审核        │
└────────┬─────────┘ └────────┬─────────┘ │ · 数据回调        │
         │                    │            └────────┬─────────┘
         ▼                    ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         基础设施层                                       │
│  MinIO/OSS  ·  MySQL  ·  Redis  ·  RabbitMQ/Kafka  ·  Elasticsearch   │
└─────────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       外部系统 (ERP / WMS / 财务系统等)                   │
│                         通过回调接口接收识别结果                           │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 微服务划分

| 服务名 | 职责 | 技术选型 |
|--------|------|----------|
| `ocr-gateway` | API网关，统一入口，限流鉴权 | Spring Cloud Gateway |
| `ocr-file-service` | 文件上传、存储、格式转换、PDF拆页 | Spring Boot + MinIO |
| `ocr-recognition-service` | OCR识别核心，图像预处理，多引擎调度 | Spring Boot + PaddleOCR/Tesseract |
| `ocr-business-service` | 对象配置、字段管理、供应商适配、任务管理、人工审核 | Spring Boot + MyBatis-Plus |
| `ocr-callback-service` | 识别结果回调外部系统 | Spring Boot + 消息队列 |
| `ocr-common` | 公共模块（工具类、DTO、异常定义等） | Java Library |

---

## 3. 核心流程设计

### 3.1 主流程 — 文件识别全链路

```
用户上传文件（单个/批量）
        │
        ▼
  ┌─────────────┐
  │ 文件接收存储  │ ← 存入MinIO，生成文件记录
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 文件预处理   │ ← PDF拆页、图片格式标准化
  └──────┬──────┘
         │
         ▼
  ┌─────────────┐
  │ 图像增强处理  │ ← 去噪、纠偏、二值化、对比度增强、去模糊
  └──────┬──────┘
         │
         ▼
  ┌─────────────────────────────┐
  │  OCR引擎识别（多引擎策略）    │
  │  · 机打 → PaddleOCR         │
  │  · 手写 → 手写识别模型       │
  │  · 表格 → 表格专用模型       │
  └──────────┬──────────────────┘
             │
             ▼
  ┌──────────────────────┐
  │ 结构化数据提取         │ ← 根据对象定义提取表头/表体字段
  │ · 字段智能匹配         │ ← 处理不同供应商字段名称差异
  │ · 置信度计算           │ ← 每个字段独立计算置信度
  └──────────┬───────────┘
             │
             ▼
      ┌──────────────┐
      │ 置信度判断     │
      │ 所有字段100%？ │
      └──┬────────┬──┘
        Yes       No
         │         │
         ▼         ▼
  ┌──────────┐ ┌──────────────┐
  │ 自动回调  │ │ 进入人工审核  │
  │ 外部系统  │ │ 队列          │
  └──────────┘ └──────┬───────┘
                      │
                      ▼
              ┌──────────────┐
              │ 用户人工修正  │ ← 低置信度字段红色高亮
              │ 确认提交      │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ 回调外部系统  │
              └──────────────┘
```

### 3.2 置信度评估策略

```
置信度级别定义：
  ├─ 高置信度 (≥95%)  → 绿色显示，可自动通过
  ├─ 中置信度 (80%-95%) → 橙色显示，需人工确认
  └─ 低置信度 (<80%)   → 红色显示，需人工修正

自动通过阈值：
  └─ 所有字段置信度均≥设定阈值（默认95%，可配置）→ 自动回调
  └─ 任一字段低于阈值 → 转入人工审核
```

> **注意**: 用户需求中提到"100%识别率才自动通过"，在实际实现中建议使用可配置的阈值（默认95%），因为OCR引擎很难对所有字段都给出100%置信度。此阈值可在系统中灵活调整。

---

## 4. 核心模块详细设计

### 4.1 对象与字段配置模块

#### 4.1.1 数据模型

```
┌─────────────────────────────────────────────────────┐
│                  ocr_document_type                   │
│  (单据类型定义，如：采购进货单、进项发票、询价清单)      │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  type_code       VARCHAR(50)    类型编码              │
│  type_name       VARCHAR(100)   类型名称              │
│  description     VARCHAR(500)   描述                 │
│  auto_threshold  DECIMAL(5,2)   自动通过阈值(默认95)   │
│  status          TINYINT        启用状态              │
│  created_at      DATETIME       创建时间              │
│  updated_at      DATETIME       更新时间              │
└─────────────────────────────────────────────────────┘
                    │ 1:N
                    ▼
┌─────────────────────────────────────────────────────┐
│                ocr_field_definition                  │
│  (字段定义，分表头字段和表体字段)                       │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  doc_type_id     BIGINT         所属单据类型           │
│  field_code      VARCHAR(50)    字段编码(系统内部用)    │
│  field_name      VARCHAR(100)   字段名称(默认显示名)    │
│  field_type      VARCHAR(20)    数据类型               │
│  │                              (STRING/NUMBER/DATE   │
│  │                               /AMOUNT/ENUM)        │
│  position        VARCHAR(10)    位置: HEADER/BODY     │
│  is_required     TINYINT        是否必填               │
│  sort_order      INT            排序                  │
│  validation_rule VARCHAR(200)   校验规则(正则等)        │
│  default_value   VARCHAR(200)   默认值                 │
│  status          TINYINT        启用状态               │
└─────────────────────────────────────────────────────┘
                    │ 1:N
                    ▼
┌─────────────────────────────────────────────────────┐
│              ocr_field_alias                         │
│  (字段别名映射——处理不同供应商字段名差异)               │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  field_id        BIGINT         所属字段定义           │
│  supplier_id     BIGINT         供应商ID(NULL=通用)    │
│  alias_name      VARCHAR(100)   别名                 │
│  priority        INT            优先级(多个别名时)      │
└─────────────────────────────────────────────────────┘
```

#### 4.1.2 字段别名匹配机制

不同供应商的单据字段名可能不同，例如：
- "供应商名称" / "供货商" / "vendor" / "供方"  → 系统字段 `supplier_name`
- "金额" / "合计金额" / "总价" / "price" → 系统字段 `total_amount`

匹配策略（优先级从高到低）：
1. **精确匹配**: OCR识别的字段名与别名完全一致
2. **模糊匹配**: 使用编辑距离算法（Levenshtein Distance），相似度>80%
3. **语义匹配**: 使用同义词库/NLP模型进行语义相似度匹配
4. **位置匹配**: 根据字段在文档中的相对位置推断（表格列位置）
5. **历史学习**: 同一供应商历次识别成功的映射关系缓存

### 4.2 供应商模板管理

```
┌─────────────────────────────────────────────────────┐
│              ocr_supplier_template                   │
│  (供应商模板——学习并存储每个供应商的单据特征)            │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  supplier_id     BIGINT         供应商ID              │
│  doc_type_id     BIGINT         单据类型              │
│  template_name   VARCHAR(100)   模板名称              │
│  layout_feature  JSON           布局特征(字段位置等)    │
│  field_mapping   JSON           字段映射关系           │
│  sample_file_url VARCHAR(500)   样本文件地址           │
│  match_count     INT            累计匹配次数           │
│  status          TINYINT        启用状态              │
│  created_at      DATETIME       创建时间              │
│  updated_at      DATETIME       更新时间              │
└─────────────────────────────────────────────────────┘
```

`field_mapping` JSON 示例：
```json
{
  "header": {
    "供货商": "supplier_name",
    "订单号码": "order_no",
    "日期": "order_date",
    "合计": "total_amount"
  },
  "body": {
    "品名": "product_name",
    "规格型号": "specification",
    "数量": "quantity",
    "单价": "unit_price",
    "金额": "amount"
  }
}
```

### 4.3 OCR识别引擎设计

#### 4.3.1 多引擎调度策略

```
                 ┌──────────────┐
                 │ 文件类型检测   │
                 └──────┬───────┘
                        │
            ┌───────────┼───────────┐
            ▼           ▼           ▼
        ┌───────┐  ┌────────┐  ┌────────┐
        │  PDF  │  │ 图片    │  │ 其他   │
        └───┬───┘  └───┬────┘  └───┬────┘
            │          │           │
            ▼          ▼           ▼
     ┌──────────┐ ┌──────────┐   拒绝
     │PDF解析器  │ │图像分析器 │
     │          │ │          │
     │·文本PDF  │ │·机打检测  │
     │ →直接提取│ │·手写检测  │
     │·扫描PDF  │ │·表格检测  │
     │ →转图片  │ │·质量评估  │
     └────┬─────┘ └────┬─────┘
          │            │
          ▼            ▼
   ┌─────────────────────────────┐
   │      图像预处理流水线         │
   │  ┌─────┐ ┌─────┐ ┌──────┐  │
   │  │去噪  │→│纠偏  │→│增强   │  │
   │  └─────┘ └─────┘ └──────┘  │
   │  ┌─────┐ ┌─────┐ ┌──────┐  │
   │  │二值化│→│锐化  │→│缩放   │  │
   │  └─────┘ └─────┘ └──────┘  │
   └──────────┬──────────────────┘
              │
              ▼
   ┌─────────────────────────────┐
   │      OCR引擎选择与调用       │
   │                             │
   │  ┌───────────────────────┐  │
   │  │ PaddleOCR (主引擎)     │  │
   │  │ · 通用文字识别          │  │
   │  │ · 表格识别             │  │
   │  │ · 版面分析             │  │
   │  └───────────────────────┘  │
   │  ┌───────────────────────┐  │
   │  │ Tesseract (备用引擎)   │  │
   │  │ · 辅助验证             │  │
   │  └───────────────────────┘  │
   │  ┌───────────────────────┐  │
   │  │ 商业API (可选)         │  │
   │  │ · 百度OCR / 腾讯OCR   │  │
   │  │ · 用于高难度场景兜底    │  │
   │  └───────────────────────┘  │
   └─────────────────────────────┘
```

#### 4.3.2 Java调用PaddleOCR方案

由于PaddleOCR是Python生态，Java项目有以下集成方案：

| 方案 | 描述 | 优势 | 劣势 |
|------|------|------|------|
| **方案A: HTTP微服务** | PaddleOCR封装为独立Python HTTP服务，Java通过HTTP调用 | 解耦、独立扩缩容、技术栈隔离 | 网络开销 |
| **方案B: PaddleOCR Java SDK** | 使用PaddlePaddle的Java推理库Paddle Inference | 纯Java、无跨语言 | 模型部署复杂 |
| **方案C: ONNX Runtime** | 将PaddleOCR模型转为ONNX格式，用Java ONNX Runtime推理 | 纯Java、性能好 | 转换可能损失精度 |

**推荐方案A**: PaddleOCR作为独立Python服务，通过HTTP/gRPC与Java服务交互。好处：
- OCR服务可独立扩缩容，满足高并发
- 可利用PaddleOCR完整生态（最新模型、预处理Pipeline）
- Java服务专注业务逻辑

### 4.4 图像预处理Pipeline

```java
// 预处理链（责任链模式）
public interface ImageProcessor {
    ProcessResult process(BufferedImage image, ProcessContext context);
}

// 预处理步骤：
// 1. 格式标准化 → 统一转为PNG/JPEG
// 2. 分辨率调整 → 确保DPI≥300
// 3. 灰度化
// 4. 去噪（高斯滤波/中值滤波）
// 5. 纠偏（霍夫变换检测文本行倾斜角度）
// 6. 二值化（自适应阈值 OTSU）
// 7. 对比度增强（CLAHE算法）
// 8. 锐化处理
```

### 4.5 结构化数据提取

#### 4.5.1 处理流程

```
OCR原始识别结果（文本+坐标+置信度）
        │
        ▼
┌──────────────────┐
│  版面分析         │ ← 区分表头区域 / 表体区域 / 无关区域
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  表格结构识别      │ ← 识别表格的行列结构
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Key-Value提取    │ ← 表头区域：提取"标签:值"对
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  字段映射匹配     │ ← 将识别的标签映射到系统定义字段
│                  │    （使用别名库+模糊匹配+NLP）
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  数据类型转换     │ ← 日期格式化、金额解析、数值提取
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  校验与置信度计算  │ ← 正则校验、逻辑校验、交叉验证
└────────┬─────────┘
         │
         ▼
  结构化识别结果JSON
```

#### 4.5.2 识别结果数据结构

```json
{
  "taskId": "T202601010001",
  "fileId": "F001",
  "fileName": "进货单_供应商A_20260101.pdf",
  "docTypeCode": "PURCHASE_ORDER",
  "docTypeName": "采购进货单",
  "supplierId": 1001,
  "supplierName": "供应商A",
  "overallConfidence": 92.5,
  "autoApproved": false,
  "header": [
    {
      "fieldCode": "order_no",
      "fieldName": "订单编号",
      "value": "PO20260101001",
      "confidence": 99.2,
      "status": "HIGH",
      "ocrRawText": "PO20260101001",
      "boundingBox": [120, 50, 350, 80]
    },
    {
      "fieldCode": "supplier_name",
      "fieldName": "供应商名称",
      "value": "深圳市XX有限公司",
      "confidence": 75.3,
      "status": "LOW",
      "ocrRawText": "深圳市X X有限公 司",
      "boundingBox": [120, 90, 400, 120]
    }
  ],
  "body": [
    {
      "rowIndex": 0,
      "cells": [
        {
          "fieldCode": "product_name",
          "fieldName": "商品名称",
          "value": "电子元器件A",
          "confidence": 98.0,
          "status": "HIGH"
        },
        {
          "fieldCode": "quantity",
          "fieldName": "数量",
          "value": "1000",
          "confidence": 95.5,
          "status": "HIGH"
        },
        {
          "fieldCode": "unit_price",
          "fieldName": "单价",
          "value": "12.50",
          "confidence": 88.0,
          "status": "MEDIUM"
        }
      ]
    }
  ]
}
```

---

## 5. 数据库设计

### 5.1 核心表结构

#### 识别任务表

```
┌─────────────────────────────────────────────────────┐
│                  ocr_recognition_task                │
│  (识别任务——一次批量上传为一个任务)                     │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  task_no         VARCHAR(50)    任务编号              │
│  doc_type_id     BIGINT         单据类型ID            │
│  supplier_id     BIGINT         供应商ID (可选)       │
│  total_files     INT            文件总数              │
│  success_count   INT            识别成功数             │
│  failed_count    INT            识别失败数             │
│  pending_count   INT            待审核数              │
│  status          VARCHAR(20)    任务状态               │
│  │                              UPLOADING/PROCESSING  │
│  │                              /PARTIAL_DONE/DONE    │
│  callback_url    VARCHAR(500)   回调地址              │
│  callback_status VARCHAR(20)    回调状态              │
│  created_by      VARCHAR(50)    创建人               │
│  created_at      DATETIME       创建时间              │
│  updated_at      DATETIME       更新时间              │
└─────────────────────────────────────────────────────┘
```

#### 识别文件表

```
┌─────────────────────────────────────────────────────┐
│                  ocr_recognition_file                │
│  (识别文件——每个上传的文件一条记录)                     │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  task_id         BIGINT         所属任务ID            │
│  file_name       VARCHAR(200)   原始文件名            │
│  file_url        VARCHAR(500)   文件存储地址           │
│  file_type       VARCHAR(20)    文件类型(PDF/JPG/PNG) │
│  file_size       BIGINT         文件大小(字节)         │
│  page_count      INT            页数(PDF)             │
│  status          VARCHAR(20)    状态                  │
│  │                              PENDING/PREPROCESSING │
│  │                              /RECOGNIZING/SUCCESS  │
│  │                              /NEED_REVIEW/FAILED   │
│  overall_conf    DECIMAL(5,2)   整体置信度             │
│  result_json     JSON           识别结果JSON          │
│  review_json     JSON           审核修改后JSON         │
│  error_message   VARCHAR(500)   错误信息              │
│  ocr_engine      VARCHAR(50)    使用的OCR引擎          │
│  process_time    INT            处理耗时(毫秒)         │
│  reviewed_by     VARCHAR(50)    审核人                │
│  reviewed_at     DATETIME       审核时间              │
│  created_at      DATETIME       创建时间              │
│  updated_at      DATETIME       更新时间              │
└─────────────────────────────────────────────────────┘
```

#### 识别结果明细表

```
┌─────────────────────────────────────────────────────┐
│              ocr_recognition_detail                  │
│  (识别结果字段明细)                                    │
├─────────────────────────────────────────────────────┤
│  id              BIGINT         主键                 │
│  file_id         BIGINT         所属文件ID            │
│  field_def_id    BIGINT         字段定义ID            │
│  position        VARCHAR(10)    HEADER/BODY          │
│  row_index       INT            行索引(表体用)         │
│  ocr_raw_text    VARCHAR(500)   OCR原始识别文本        │
│  parsed_value    VARCHAR(500)   解析后的值             │
│  confidence      DECIMAL(5,2)   置信度                │
│  status          VARCHAR(10)    HIGH/MEDIUM/LOW       │
│  is_modified     TINYINT        是否被人工修改         │
│  modified_value  VARCHAR(500)   修改后的值             │
│  bounding_box    VARCHAR(100)   文字区域坐标           │
│  created_at      DATETIME       创建时间              │
│  updated_at      DATETIME       更新时间              │
└─────────────────────────────────────────────────────┘
```

### 5.2 完整ER关系图

```
ocr_document_type  1───N  ocr_field_definition  1───N  ocr_field_alias
       │                          │
       │1                         │
       │                          │
       N                          │
ocr_supplier_template             │
       │                          │
       │                          │
ocr_recognition_task  1───N  ocr_recognition_file  1───N  ocr_recognition_detail
                                                              │
                                                              N
                                                              │
                                                              1
                                                     ocr_field_definition
```

---

## 6. API接口设计

### 6.1 文件上传与识别

```
POST /api/v1/ocr/recognize
Content-Type: multipart/form-data

参数：
  files[]          File[]       文件列表（支持多文件）
  docTypeCode      String       单据类型编码
  supplierId       Long         供应商ID（可选）
  callbackUrl      String       回调地址
  autoApprove      Boolean      是否自动通过（默认true）

响应：
{
  "code": 200,
  "data": {
    "taskId": "T202601010001",
    "taskNo": "OCR20260101001",
    "totalFiles": 5,
    "status": "PROCESSING",
    "estimatedTime": 30
  }
}
```

### 6.2 查询识别结果

```
GET /api/v1/ocr/task/{taskId}

响应：
{
  "code": 200,
  "data": {
    "taskId": "T202601010001",
    "status": "PARTIAL_DONE",
    "totalFiles": 5,
    "successCount": 3,
    "pendingCount": 2,
    "files": [
      {
        "fileId": "F001",
        "fileName": "发票_001.pdf",
        "status": "SUCCESS",
        "overallConfidence": 98.5
      },
      {
        "fileId": "F002",
        "fileName": "发票_002.jpg",
        "status": "NEED_REVIEW",
        "overallConfidence": 82.3
      }
    ]
  }
}
```

### 6.3 获取待审核文件详情

```
GET /api/v1/ocr/file/{fileId}/review

响应：
{
  "code": 200,
  "data": {
    "fileId": "F002",
    "fileName": "发票_002.jpg",
    "fileUrl": "https://...",
    "docTypeName": "采购进货单",
    "overallConfidence": 82.3,
    "header": [ ... ],      // 表头字段列表（含置信度）
    "body": [ ... ]          // 表体行列表（含每个单元格置信度）
  }
}
```

### 6.4 提交审核修改

```
POST /api/v1/ocr/file/{fileId}/confirm

请求体：
{
  "header": [
    {
      "fieldCode": "supplier_name",
      "value": "深圳市XX科技有限公司"
    }
  ],
  "body": [
    {
      "rowIndex": 0,
      "cells": [
        {
          "fieldCode": "unit_price",
          "value": "12.80"
        }
      ]
    }
  ]
}

响应：
{
  "code": 200,
  "message": "审核确认成功，数据已推送至外部系统"
}
```

### 6.5 对象类型管理

```
POST   /api/v1/config/doc-type            创建单据类型
PUT    /api/v1/config/doc-type/{id}        更新单据类型
GET    /api/v1/config/doc-type             查询单据类型列表
DELETE /api/v1/config/doc-type/{id}        删除单据类型

POST   /api/v1/config/field               创建字段定义
PUT    /api/v1/config/field/{id}           更新字段定义
GET    /api/v1/config/doc-type/{id}/fields 查询类型下的字段
DELETE /api/v1/config/field/{id}           删除字段定义

POST   /api/v1/config/field-alias         创建字段别名
GET    /api/v1/config/field/{id}/aliases   查询字段别名
DELETE /api/v1/config/field-alias/{id}     删除字段别名
```

### 6.6 回调外部系统接口规范

```
POST {callbackUrl}

请求体：
{
  "taskId": "T202601010001",
  "taskNo": "OCR20260101001",
  "docTypeCode": "PURCHASE_ORDER",
  "fileId": "F001",
  "fileName": "进货单_001.pdf",
  "isAutoApproved": true,
  "data": {
    "header": {
      "order_no": "PO20260101001",
      "supplier_name": "深圳市XX有限公司",
      "order_date": "2026-01-01",
      "total_amount": "125000.00"
    },
    "body": [
      {
        "product_name": "电子元器件A",
        "specification": "TYPE-C 3.0",
        "quantity": "1000",
        "unit_price": "12.50",
        "amount": "12500.00"
      }
    ]
  },
  "timestamp": "2026-01-01T10:30:00Z",
  "sign": "sha256签名"
}
```

---

## 7. 前端设计

### 7.1 技术选型

| 组件 | PC端 | H5端 |
|------|------|------|
| 框架 | Vue 3 | Vue 3 |
| UI库 | Element Plus | Vant 4 |
| 构建 | Vite | Vite |
| 状态管理 | Pinia | Pinia |
| HTTP | Axios | Axios |

采用**响应式设计 + 组件适配**策略，共享核心业务逻辑，UI组件按端适配。

### 7.2 页面设计

#### 页面1: 文件上传页

```
┌──────────────────────────────────────────────────┐
│  OCR智能识别                                      │
├──────────────────────────────────────────────────┤
│                                                  │
│  单据类型: [采购进货单     ▼]                      │
│  供应商:   [请选择供应商   ▼]  (可选)              │
│                                                  │
│  ┌──────────────────────────────────────────┐    │
│  │                                          │    │
│  │     📄 拖拽文件到此处或点击上传             │    │
│  │     支持 PDF / JPG / PNG                 │    │
│  │     单次最多上传 50 个文件                 │    │
│  │                                          │    │
│  └──────────────────────────────────────────┘    │
│                                                  │
│  已选文件:                                        │
│  ┌──────────────────────────────────────────┐    │
│  │ ✓ 进货单_001.pdf    2.3MB    [删除]       │    │
│  │ ✓ 进货单_002.jpg    1.1MB    [删除]       │    │
│  │ ✓ 进货单_003.png    0.8MB    [删除]       │    │
│  └──────────────────────────────────────────┘    │
│                                                  │
│  [开始识别]                                       │
│                                                  │
└──────────────────────────────────────────────────┘
```

#### 页面2: 任务进度页

```
┌──────────────────────────────────────────────────┐
│  识别任务: OCR20260101001                         │
├──────────────────────────────────────────────────┤
│                                                  │
│  总进度: ████████████░░░░ 75% (3/4)              │
│                                                  │
│  ┌────────────────────────────────────────────┐  │
│  │ 文件名          状态      置信度    操作    │  │
│  ├────────────────────────────────────────────┤  │
│  │ 进货单_001.pdf  ✅ 已完成   98.5%          │  │
│  │ 进货单_002.jpg  ⚠️ 待审核   82.3%   [审核] │  │
│  │ 进货单_003.png  🔄 识别中    --             │  │
│  │ 进货单_004.pdf  ❌ 失败     --      [重试] │  │
│  └────────────────────────────────────────────┘  │
│                                                  │
└──────────────────────────────────────────────────┘
```

#### 页面3: 人工审核页（核心页面）

```
┌──────────────────────────────────────────────────────────────────┐
│  人工审核 - 进货单_002.jpg                    [上一个] [下一个]    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────┐  ┌──────────────────────────────────┐  │
│  │                     │  │  表头信息                          │  │
│  │                     │  │  ┌──────────────────────────────┐ │  │
│  │                     │  │  │ 订单编号: PO20260101001      │ │  │
│  │    原始图片/PDF      │  │  │ 订单日期: 2026-01-01        │ │  │
│  │    预览区域          │  │  │ 供应商:  [深圳市XX有限公司]   │ │  │
│  │                     │  │  │          ↑ 红色边框,低置信度  │ │  │
│  │  (支持缩放/拖拽)     │  │  │ 联系人:  张三               │ │  │
│  │                     │  │  └──────────────────────────────┘ │  │
│  │  点击字段时          │  │                                  │  │
│  │  自动定位到对应区域   │  │  表体信息                          │  │
│  │  并高亮显示          │  │  ┌──────┬──────┬─────┬──────────┐│  │
│  │                     │  │  │品名  │数量  │单价  │金额       ││  │
│  │                     │  │  ├──────┼──────┼─────┼──────────┤│  │
│  │                     │  │  │元件A │1000  │12.50│ 12500    ││  │
│  │                     │  │  │元件B │[500] │8.00 │ [40 00]  ││  │
│  │                     │  │  │ ↑正常  ↑橙色   ↑正常  ↑红色    ││  │
│  └─────────────────────┘  │  └──────┴──────┴─────┴──────────┘│  │
│                           └──────────────────────────────────┘  │
│                                                                  │
│  字段颜色说明: 🟢高置信度(≥95%)  🟠中置信度(80-95%)  🔴低置信度(<80%) │
│                                                                  │
│                            [确认提交]    [跳过]                    │
└──────────────────────────────────────────────────────────────────┘

审核交互要点：
  · 左侧显示原始文件预览，右侧显示结构化数据
  · 低置信度字段红色高亮，可编辑修改
  · 点击右侧字段时，左侧自动定位到对应区域
  · 修改后的字段标记为"已修改"
  · 支持键盘 Tab 键快速在字段间切换
```

---

## 8. 高并发架构设计（1万并发）

### 8.1 整体策略

```
                            ┌─────────────┐
                            │  Nginx      │
                            │  负载均衡    │
                            │  (4台)      │
                            └──────┬──────┘
                                   │
                    ┌──────────────┼──────────────┐
                    ▼              ▼              ▼
             ┌──────────┐  ┌──────────┐  ┌──────────┐
             │ Gateway  │  │ Gateway  │  │ Gateway  │
             │ (3实例)  │  │ (3实例)  │  │ (3实例)  │
             └────┬─────┘  └────┬─────┘  └────┬─────┘
                  │             │             │
                  └──────┬──────┘──────┬──────┘
                         │             │
          ┌──────────────┤             ├──────────────┐
          ▼              ▼             ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌──────────┐ ┌──────────┐
   │File Service│ │File Service│ │Biz Svc   │ │Biz Svc   │
   │ (5实例)    │ │ (5实例)    │ │ (5实例)  │ │ (5实例)  │
   └─────┬──────┘ └─────┬──────┘ └────┬─────┘ └────┬─────┘
         │              │             │             │
         └──────┬───────┘             └──────┬──────┘
                │                            │
                ▼                            │
         ┌────────────┐                      │
         │ 消息队列    │ ◄───────────────────┘
         │ RabbitMQ   │
         │ (集群)     │
         └─────┬──────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐
│OCR Svc │ │OCR Svc │ │OCR Svc │    ← OCR识别服务集群
│(10实例)│ │(10实例)│ │(10实例)│       (可GPU加速)
└────────┘ └────────┘ └────────┘
```

### 8.2 关键设计点

| 策略 | 实现方式 |
|------|----------|
| **异步处理** | 文件上传后立即返回任务ID，OCR识别通过消息队列异步处理 |
| **消息队列** | RabbitMQ/Kafka 解耦上传与识别，削峰填谷 |
| **弹性伸缩** | OCR服务支持K8s HPA，根据队列积压自动扩缩容 |
| **文件存储** | MinIO分布式存储，支持高并发读写 |
| **缓存** | Redis缓存供应商模板、字段映射、热点数据 |
| **数据库** | MySQL读写分离 + 分表（按月份分表） |
| **限流** | Gateway层Sentinel限流，保护后端服务 |
| **WebSocket** | 实时推送识别进度给前端 |

### 8.3 并发能力估算

```
假设条件：
  · 单个文件OCR处理平均耗时: 3秒
  · OCR服务实例数: 30个
  · 每实例并发处理: 10个请求

理论吞吐量:
  · 30 × 10 / 3 = 100 文件/秒
  · 每小时处理: 360,000 文件

并发支持:
  · 前端接口层（上传+查询）: Gateway 3实例 × 2000并发 = 6000+
  · 配合Nginx层: 轻松支持 10,000并发连接
  · OCR处理: 异步队列缓冲，不受并发限制

峰值场景:
  · 10,000用户同时上传 → 文件快速入队
  · OCR服务按队列消费速度处理
  · 用户通过WebSocket获取实时进度
```

---

## 9. 技术栈总览

### 后端

| 组件 | 技术 | 版本 |
|------|------|------|
| 基础框架 | Spring Boot | 3.2+ |
| 微服务 | Spring Cloud | 2023.x |
| 网关 | Spring Cloud Gateway | - |
| ORM | MyBatis-Plus | 3.5+ |
| 数据库 | MySQL | 8.0+ |
| 缓存 | Redis | 7.0+ |
| 消息队列 | RabbitMQ | 3.12+ |
| 文件存储 | MinIO | 最新 |
| OCR引擎 | PaddleOCR (Python服务) | 2.7+ |
| 备用OCR | Tesseract | 5.x |
| 服务注册 | Nacos | 2.3+ |
| 限流熔断 | Sentinel | 1.8+ |
| 容器化 | Docker + Kubernetes | - |
| 搜索引擎 | Elasticsearch (日志/审计) | 8.x |

### 前端

| 组件 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 | 3.4+ |
| PC端UI | Element Plus | 2.5+ |
| H5端UI | Vant | 4.x |
| 构建工具 | Vite | 5.x |
| 状态管理 | Pinia | 2.x |
| HTTP客户端 | Axios | 1.x |
| 图片预览 | viewerjs | - |
| PDF预览 | pdf.js | - |
| WebSocket | socket.io-client | - |

---

## 10. 项目模块结构

```
ocr-agent/
├── ocr-gateway/                    # API网关
│   └── src/main/java/
│       └── com.ocr.gateway/
│           ├── config/             # 路由、限流配置
│           └── filter/             # 全局过滤器
│
├── ocr-common/                     # 公共模块
│   └── src/main/java/
│       └── com.ocr.common/
│           ├── dto/                # 数据传输对象
│           ├── enums/              # 枚举
│           ├── exception/          # 异常定义
│           ├── result/             # 统一响应
│           └── utils/              # 工具类
│
├── ocr-file-service/               # 文件服务
│   └── src/main/java/
│       └── com.ocr.file/
│           ├── controller/         # 文件上传接口
│           ├── service/            # 文件处理逻辑
│           ├── storage/            # MinIO存储
│           └── converter/          # PDF转图片等
│
├── ocr-recognition-service/        # OCR识别服务
│   └── src/main/java/
│       └── com.ocr.recognition/
│           ├── consumer/           # MQ消费者
│           ├── engine/             # OCR引擎调用
│           │   ├── PaddleOcrEngine
│           │   ├── TesseractEngine
│           │   └── OcrEngineFactory
│           ├── preprocess/         # 图像预处理
│           ├── parser/             # 结果解析
│           │   ├── LayoutAnalyzer  # 版面分析
│           │   ├── TableParser     # 表格解析
│           │   └── KvExtractor     # Key-Value提取
│           ├── matcher/            # 字段匹配
│           │   ├── ExactMatcher
│           │   ├── FuzzyMatcher
│           │   └── SemanticMatcher
│           └── confidence/         # 置信度计算
│
├── ocr-business-service/           # 业务管理服务
│   └── src/main/java/
│       └── com.ocr.business/
│           ├── controller/
│           │   ├── DocTypeController    # 单据类型管理
│           │   ├── FieldController      # 字段管理
│           │   ├── TaskController       # 任务管理
│           │   └── ReviewController     # 审核管理
│           ├── service/
│           ├── mapper/
│           └── entity/
│
├── ocr-callback-service/           # 回调服务
│   └── src/main/java/
│       └── com.ocr.callback/
│           ├── consumer/           # MQ消费者
│           ├── sender/             # HTTP回调发送
│           └── retry/              # 重试机制
│
├── ocr-python-service/             # PaddleOCR Python服务
│   ├── app/
│   │   ├── main.py                 # FastAPI入口
│   │   ├── ocr_engine.py           # PaddleOCR封装
│   │   ├── preprocessor.py         # 图像预处理
│   │   ├── table_recognizer.py     # 表格识别
│   │   └── models/                 # 模型文件
│   ├── Dockerfile
│   └── requirements.txt
│
├── ocr-frontend/                   # 前端项目
│   ├── src/
│   │   ├── views/
│   │   │   ├── upload/             # 文件上传页
│   │   │   ├── task/               # 任务列表页
│   │   │   ├── review/             # 人工审核页
│   │   │   └── config/             # 系统配置页
│   │   ├── components/
│   │   │   ├── pc/                 # PC端组件
│   │   │   └── h5/                 # H5端组件
│   │   ├── api/                    # API调用
│   │   ├── stores/                 # Pinia状态
│   │   └── utils/
│   ├── package.json
│   └── vite.config.ts
│
├── deploy/                         # 部署配置
│   ├── docker/
│   │   ├── docker-compose.yml
│   │   └── Dockerfile.*
│   ├── k8s/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── hpa.yaml
│   └── nginx/
│       └── nginx.conf
│
├── docs/                           # 文档
│   ├── api/                        # API文档
│   └── sql/                        # 数据库脚本
│
└── pom.xml                         # 父POM
```

---

## 11. 部署架构

```
┌─────────────────────────────────────────────────────┐
│                   Kubernetes Cluster                 │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Ingress  │  │ Nginx    │  │ 监控              │  │
│  │ Ctrl     │  │ (LB)    │  │ Prometheus+Grafana│  │
│  └────┬─────┘  └────┬─────┘  └──────────────────┘  │
│       │              │                              │
│  ┌────┴──────────────┴─────────────────────┐        │
│  │            Service Mesh                  │        │
│  │  ┌─────────┐ ┌──────────┐ ┌──────────┐  │        │
│  │  │Gateway  │ │File Svc  │ │Biz Svc   │  │        │
│  │  │×3       │ │×5        │ │×5        │  │        │
│  │  └─────────┘ └──────────┘ └──────────┘  │        │
│  │  ┌─────────┐ ┌──────────┐ ┌──────────┐  │        │
│  │  │OCR Svc  │ │Callback  │ │Python OCR│  │        │
│  │  │×10      │ │×3        │ │×30(GPU)  │  │        │
│  │  └─────────┘ └──────────┘ └──────────┘  │        │
│  └─────────────────────────────────────────┘        │
│                                                     │
│  ┌────────────────────────────────────────┐          │
│  │          中间件层 (StatefulSet)          │          │
│  │  MySQL(主从) Redis(集群) RabbitMQ Nacos │          │
│  │  MinIO(集群) Elasticsearch              │          │
│  └────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────┘
```

---

## 12. 安全设计

| 安全策略 | 实现方式 |
|----------|----------|
| 接口鉴权 | JWT Token + Spring Security |
| 文件安全 | 文件类型白名单校验、文件大小限制、病毒扫描 |
| 数据传输 | HTTPS/TLS加密 |
| 回调安全 | SHA256签名校验 |
| 敏感数据 | 发票号码等敏感信息脱敏展示 |
| 操作审计 | 所有人工审核操作记录审计日志 |
| 限流防刷 | Sentinel + IP限流 + 用户级限流 |

---

## 13. 监控与告警

| 监控维度 | 工具 | 关键指标 |
|----------|------|----------|
| 服务监控 | Prometheus + Grafana | QPS、响应时间、错误率 |
| 链路追踪 | SkyWalking | 全链路耗时、异常定位 |
| 日志监控 | ELK Stack | 错误日志、识别失败分析 |
| 业务监控 | 自定义Dashboard | 识别成功率、平均置信度、人工审核率 |
| 队列监控 | RabbitMQ Management | 队列积压量、消费速度 |

---

## 14. 开发计划（建议）

| 阶段 | 内容 | 建议工期 |
|------|------|----------|
| **第一阶段** | 基础框架搭建 + 对象/字段配置模块 + 文件上传 | 2周 |
| **第二阶段** | PaddleOCR Python服务 + Java OCR服务集成 + 图像预处理 | 3周 |
| **第三阶段** | 结构化提取 + 字段匹配 + 置信度评估 | 2周 |
| **第四阶段** | 前端开发（上传页+进度页+审核页）PC端 + H5适配 | 3周 |
| **第五阶段** | 回调机制 + 批量处理 + 供应商模板学习 | 2周 |
| **第六阶段** | 高并发优化 + 压测 + 部署 | 2周 |
| **总计** | | **约14周** |

---

## 15. 风险与对策

| 风险 | 对策 |
|------|------|
| 手写体识别率低 | 引入专用手写识别模型，结合商业API兜底 |
| 模糊/扭曲文档 | 多级图像预处理Pipeline，增强后再识别 |
| 不同供应商模板差异大 | 建立模板学习机制，历史匹配自动优化 |
| OCR引擎性能瓶颈 | GPU加速 + 服务水平扩展 + 异步队列缓冲 |
| 表格结构复杂 | 使用PaddleOCR的表格识别模型 + 规则补充 |
| 100%准确率难达标 | 设置可配置阈值，结合人工审核兜底 |

---

## 16. 待确认事项

请确认以下事项，确认后即可开始开发：

1. **阈值设置**: 自动通过的置信度阈值建议设为95%（可配置），严格100%可能导致大部分文件需人工审核，是否可接受？
2. **OCR引擎**: 是否优先使用开源方案（PaddleOCR），还是可以考虑商业API（百度/腾讯OCR）作为兜底？
3. **部署环境**: 是否已有K8s集群？是否有GPU服务器用于OCR加速？
4. **前端框架**: Vue 3 + Element Plus(PC) + Vant(H5) 是否满足需求？是否有其他前端偏好？
5. **外部系统回调**: 回调方式是HTTP还是消息队列？外部系统是否已有接收接口规范？
6. **供应商数据**: 供应商信息是从外部系统同步还是在本系统维护？
7. **用户体系**: 是否对接现有的统一认证系统（SSO），还是独立用户管理？
8. **数据留存**: 识别结果和原始文件需要保留多长时间？
9. **多语言**: 是否需要支持英文或其他语言的单据？
10. **是否需要开发成独立的前端项目**: 还是作为已有系统的一个模块嵌入？

---

*文档版本: v1.0*
*创建日期: 2026-02-23*
*作者: OCR Agent System*
